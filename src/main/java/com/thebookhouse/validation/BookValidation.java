package com.thebookhouse.validation;

import com.thebookhouse.model.Book;

public class BookValidation {
    public static boolean isValid(Book book) {
        if (book.getName().isEmpty()) {
            
        }
        return true;
    }
}
